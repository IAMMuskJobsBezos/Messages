<?xml version="1.0" encoding="utf-8"?>
<!-- Mirrors item_sent_background-ldrtl's corners exactly (square on the tail corner) so the
     overlay never reads as a square patch sitting on top of the rounded bubble. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <corners
        android:bottomLeftRadius="@dimen/small_margin"
        android:bottomRightRadius="@dimen/big_margin"
        android:topLeftRadius="@dimen/big_margin"
        android:topRightRadius="@dimen/big_margin" />

    <solid android:color="#40000000" />

</shape>
